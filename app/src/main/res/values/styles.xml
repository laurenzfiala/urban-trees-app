<resources>

    <!-- Base application theme. -->

    <style name="progressBar" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/colorSecondary</item>
        <item name="android:background">@color/colorSecondaryLight</item>
        <item name="android:progressDrawable">@drawable/progress</item>
        <item name="android:indeterminateDrawable">@drawable/progress</item>
        <item name="android:indeterminateDuration">1</item>
    </style>

    <style name="dialog" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/colorSecondary</item>
        <item name="android:background">@color/bgColor</item>
    </style>

</resources>
